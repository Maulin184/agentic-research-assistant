[project]
name = "agentic-research-assistant"
version = "0.1.0"
description = "Production-grade Agentic AI Research Assistant using LangGraph"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"